你的情況可以這樣理解：**導航不用每週改，每週只改「課程列表頁」**。下面分「策略」和「怎麼改技術」兩部分。

---

## 先定策略（配合：无固定模板 + 每周一页）

### 导航负责什么？

| 组件 | 会不会每周变 | 作用 |
|------|-------------|------|
| **`Courses ▾` 下拉** | ❌ 很少变 | 告诉学生「成功之路在哪」 |
| **进步篇 index 页** | ✅ 每周加一行 | 列出 1–36 课链接，你每周更新这里 |
| **各课 HTML** | ✅ 每周新增 | 结构可以不同，练习可有可无 |

**结论：**  
下拉选单只连到 **进步篇入口页**（或再加三册分区），不必每做一课就改全站导航。

### 三册与课次编号

```
进步篇 一  →  第  1 – 12 课  →  lesson-01.html … lesson-12.html
进步篇 二  →  第 13 – 24 课  →  lesson-13.html … lesson-24.html
进步篇 三  →  第 25 – 36 课  →  lesson-25.html … lesson-36.html
```

Anni 试作可先改名为 `lesson-01.html`，放在例如：

```
courses/road-to-success/
├── index.html          ← 课次列表（每周维护这一页）
└── lessons/
    ├── lesson-01.html  ← 原 Anni diary lesson.HTML
    ├── lesson-02.html  ← 下周新增，结构可以不同
    └── …
```

**没有固定模板没问题**——index 只是链到各 HTML，不要求每课长一样。

---

## 导航要怎么改（三步）

### 第 1 步：做一个极简 index 页

`courses/road-to-success/index.html` 内容可以很朴素，例如三个区块：

```html
<h2>成功之路 · 进步篇 一（第 1–12 课）</h2>
<ul>
  <li><a href="lessons/lesson-01.html">第 1 课 · 安妮的日记</a></li>
  <!-- 每周在这里加一行 -->
</ul>

<h2>进步篇 二（第 13–24 课）</h2>
<ul><li>（尚未发布）</li></ul>

<h2>进步篇 三（第 25–36 课）</h2>
<ul><li>（尚未发布）</li></ul>
```

**你每周的工作流：**

1. 新建 `lesson-XX.html`（AI 生成，结构随意）
2. 在 index 对应册别下 **加一行链接**
3. 全站导航 **不用动**

---

### 第 2 步：在 `nav-global.css` 加下拉样式

目前 `assets/css/nav-global.css` 只有 `.nav-link`，没有 dropdown。需要新增类似：

- `.nav-dropdown` — 外层容器（`position: relative`）
- `.nav-dropdown-toggle` — 显示「Courses ▾」
- `.nav-dropdown-menu` — 悬停/点击时显示的下拉层
- `.nav-dropdown-item` — 下拉里的每一行链接

桌面版：鼠标悬停或点击展开。  
手机版：**不必做 hover 下拉**，侧边栏里用分组即可（你已有 `mobile-menu-group`）。

---

### 第 3 步：替换各页导航 HTML

全站导航是 **复制贴在每个 HTML** 里（没有共用模板），所以要批量替换。

**桌面版** — 把原来的：

```html
<a href="courses/bct/lessons/lesson.html" class="nav-link">Lessons</a>
```

改成（概念）：

```html
<div class="nav-dropdown">
  <button class="nav-link nav-dropdown-toggle" aria-expanded="false">Courses ▾</button>
  <div class="nav-dropdown-menu">
    <a href="courses/road-to-success/index.html" class="nav-dropdown-item">成功之路 · 进步篇</a>
  </div>
</div>
```

**手机版** — 在 `Courses` 分组里，把 `Lessons` 改成：

```html
<a href="courses/road-to-success/index.html" class="mobile-menu-item">成功之路 · 进步篇</a>
```

识字（Literacy / Review Lit.）维持原样。

---

## 需要改哪些文件？

依路径深度，链接前缀不同，但改法相同：

| 位置 | 约略文件数 | 路径前缀示例 |
|------|-----------|-------------|
| 根目录 | `index.html` | `courses/road-to-success/index.html` |
| `pages/` | grammar, extras, mini-story, music, timeline | `../courses/road-to-success/index.html` |
| `pages/grammar/content/` | test.html 等 | `../../../courses/road-to-success/...` |
| `courses/shizi/` | index, lesson, review, studio 等 | `../road-to-success/index.html` |

可用搜索：`courses/bct/lessons/lesson.html` 或文字 `Lessons`，逐个替换。

**BCT 页面**（`courses/bct/`）可改可不改——已冻结，学生不会从导航进去。

---

## 下拉要做多复杂？

**建议：先最简单的一档。**

```
Courses ▾
  └── 成功之路 · 进步篇  →  index.html
```

index 里再分三册即可。  
等 13、25 课真的上线后，若想方便一点，可再加：

```
Courses ▾
  ├── 成功之路 · 进步篇 一（1–12）
  ├── 成功之路 · 进步篇 二（13–24）
  └── 成功之路 · 进步篇 三（25–36）
```

三行都连到 `index.html#book1`、`#book2`、`#book3`（页内锚点），**仍然不用 registry**。

---

## `nav-global.js` 要不要改？

建议小幅补充：

1. **下拉开关** — 点击 `Courses ▾` 展开/收起（手机桌面都适用）
2. **active 状态** — 学生在 `road-to-success/` 任一页时，让 `Courses` 显示为选中
3. 点击页面其他地方时关闭下拉

现有 `setActiveNavLink()` 只比對 `.nav-link` 的 `href`，dropdown 的 button 没有 href，需要加规则：路径包含 `road-to-success` 就标记 Courses 为 active。

---

## 和你「多尝试、练习不完整」怎么配合？

在 **COURSES_ROADMAP** 里可以把「固定模板」改成更弹性的说法：

- 每课 = 独立 HTML，**最低要求**：课文可读、有返回 index 的链接
- 练习区：**有则加 tab，无则跳过**，index 上可加小标签如「含练习」「仅课文」

index 列表示例：

```html
<li><a href="lessons/lesson-01.html">第 1 课 · 安妮的日记</a> <small>课文+词语扩展</small></li>
<li><a href="lessons/lesson-02.html">第 2 课 · …</a> <small>仅课文</small></li>
```

这样你实验不同版型时，导航结构不用跟着变。

---

## 建议执行顺序

| 顺序 | 任务 | 时间 |
|------|------|------|
| 1 | 建 `courses/road-to-success/index.html` + 把 Anni 移到 `lessons/lesson-01.html` | 一次 |
| 2 | `nav-global.css` 加 dropdown 样式 | 一次 |
| 3 | `nav-global.js` 加 dropdown 交互 + active | 一次 |
| 4 | 全站替换 `Lessons` → `Courses ▾` | 一次（约 10+ 个 HTML） |
| 5 | 每周：新课 HTML + index 加一行 | 持续 |

**现在不必做：** registry、共用 assets、固定练习模板。

---

## 一句话

- **导航**：`Courses ▾` → 进步篇 index（三册分区放在 index 里）。  
- **每周**：只维护 index 和新 `lesson-XX.html`，全站导航不动。  
- **无固定模板**：完全可行，index 只负责链到各页。

若要我在 Agent 模式直接建 index、改 CSS/JS、批量替换导航，说一声即可。